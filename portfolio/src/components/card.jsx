import React from "react";

const style = {
  width: "300px",
  height: "300px",
  borderRadius: "40px",
  backgroundColor: "grey",
};

const Card = (props) => {
  return (
    <div style={style}>
      <h>hi</h>
    </div>
  );
};

export default Card;
